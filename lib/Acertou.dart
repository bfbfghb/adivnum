import 'package:flutter/material.dart';

class Acertou extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Você acertou!"),
    );
  }
}